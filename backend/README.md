# Instant Mechanic operations API

REST + WebSocket backend for the live dashboard.

```bash
cp .env.example .env
npm install
npx prisma generate
npx prisma db push
npx tsx prisma/seed.ts
npm run dev
```

Docs: http://127.0.0.1:43124/api/docs
